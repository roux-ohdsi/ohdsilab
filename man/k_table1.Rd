% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k_table1.R
\name{k_table1}
\alias{k_table1}
\title{Generate Table 1 for Komodo Data}
\usage{
k_table1(
  con,
  cohort_table,
  write_schema = paste0("work_", keyring::key_get("db_username")),
  komodo_schema = "komodo",
  min_count = NULL
)
}
\arguments{
\item{con}{A database connection object (e.g., from \code{DatabaseConnector}).}

\item{cohort_table}{Character. Name of the cohort table. Must contain columns
\code{patient_id} and \code{index_date}.}

\item{write_schema}{Character. Schema where the cohort table is located.
Defaults to \code{"work_<db_username>"} using keyring credentials.}

\item{komodo_schema}{Character. Schema containing Komodo source tables.
Defaults to \code{"komodo"}.}

\item{min_count}{Integer or NULL. If provided, filters out rows where
\code{n_persons} is below this threshold (e.g., for small-cell suppression).}
}
\value{
A data frame with columns: \code{category}, \code{covariate},
  \code{n_persons}, and \code{percent}.
}
\description{
Creates a baseline characteristics table (Table 1) summarizing demographics
(age group, gender, race/ethnicity) for a cohort in Komodo claims data.
}
\examples{
\dontrun{
my_table1 <- k_table1(
  con,
  cohort_table = "my_cohort",
  min_count = 5
)
}
}
