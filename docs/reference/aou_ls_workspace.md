# List the current files in your workspace.

List the current files in your workspace.

## Usage

``` r
aou_ls_workspace(pattern = "*.csv")
```

## Arguments

- pattern:

  pattern like \*.csv or a single file name e.g., mydata.csv

- description:

  Quick function to list files matching a pattern in your workspace
