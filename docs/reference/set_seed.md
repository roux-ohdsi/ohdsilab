# Set a seed for reproducibility

Set a seed for reproducibility

## Usage

``` r
set_seed(seed, con = getOption("con.default.value"))
```

## Arguments

- seed:

  A seed you'd like to set - a value between 0 and 1, inclusive

- con:

  the connection to the redshift server for ohdsilab.
